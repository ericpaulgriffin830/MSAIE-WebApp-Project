# Backend TODO

- [X] Rob: Copy `backend/.env.example` to `backend/.env` and fill in the real `cafe_fausse` DB credentials (username/password) before running the Flask app locally.
- [ ] Chris: Make sure Reservation form contains fields for "Email" and "CustomerName". (DB is not Null)
- [ ] actually wiring `ReservationForm.jsx`'s `console.log` to call `fetch('/api/reserve', ...)`, and building the newsletter form itself.
