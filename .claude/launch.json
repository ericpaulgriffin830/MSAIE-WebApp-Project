{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "runtimeExecutable": "zsh",
      "runtimeArgs": [
        "-c",
        "export NVM_DIR=\"$HOME/.nvm\"; for p in \"/opt/homebrew/opt/nvm/nvm.sh\" \"/usr/local/opt/nvm/nvm.sh\" \"$NVM_DIR/nvm.sh\"; do [ -s \"$p\" ] && . \"$p\" && break; done; nvm use 22 >/dev/null 2>&1 || true; npm run dev --prefix frontend"
      ],
      "port": 5173
    }
  ]
}
